function birthdayParty(input) {

    // let hallRent = Number(input[0]);
    // let budget = 0;
    // let cake = hallRent * 0.20;
    // let drinks = hallRent * 0.20 * 0.55;
    // let animatour = hallRent / 3;

    // budget = hallRent + cake + drinks + animatour;

    console.log(Number(input) + Number(input) * 0.20 + Number(input) * 0.20 * 0.55 + Number(input) / 3);
    

}

birthdayParty(["2250"])